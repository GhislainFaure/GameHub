<!DOCTYPE html>
<html lang="fr">

<head>
  <meta charset="UTF-8">
  <title>GameHub</title>
  <link rel="stylesheet" href="/css/style.css">
  <% if(locals.cssSuplementaire) { %>
  <link rel="stylesheet" href="/css/<%= locals.cssSuplementaire %>">
  <% } %>
</head>

<body>
  <header>
    <h1>
      <a href="/">GameHub</a>
    </h1>
    <nav>
      <ul>
        <% for(const game of locals.games){ %>
          <li><a href="/game/<%= game.name %>"><%= game.title %></a></li>
          <% } %>
     
      </ul>
      
     
    </nav>
  </header>

  <main>